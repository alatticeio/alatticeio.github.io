import{_ as e,o as n,c as s,a0 as t}from"./chunks/framework.DoRPXVp_.js";const g=JSON.parse('{"title":"ICE, Relay & WireGuard Transport Design","description":"","frontmatter":{},"headers":[],"relativePath":"superpowers/specs/2026-05-01-ice-relay-wireguard-design.md","filePath":"superpowers/specs/2026-05-01-ice-relay-wireguard-design.md"}'),i={name:"superpowers/specs/2026-05-01-ice-relay-wireguard-design.md"};function r(l,a,o,d,p,c){return n(),s("div",null,[...a[0]||(a[0]=[t(`<h1 id="ice-relay-wireguard-transport-design" tabindex="-1">ICE, Relay &amp; WireGuard Transport Design <a class="header-anchor" href="#ice-relay-wireguard-transport-design" aria-label="Permalink to &quot;ICE, Relay &amp; WireGuard Transport Design&quot;">​</a></h1><blockquote><p>Status: Approved | 2026-05-01</p></blockquote><h2 id="motivation" tabindex="-1">Motivation <a class="header-anchor" href="#motivation" aria-label="Permalink to &quot;Motivation&quot;">​</a></h2><p>Lattice connects nodes across arbitrary network topologies — home LANs, corporate firewalls, cloud VPCs. A WireGuard mesh alone fails behind symmetric NAT (common in enterprise and cellular networks). The transport layer addresses this with a dual-path strategy: direct P2P via ICE when possible, relay fallback via LRP when NAT traversal fails.</p><p>This is the foundation upon which all Lattice connectivity rests. Its design priorities:</p><ol><li><strong>Lowest latency wins</strong>: ICE direct path preferred; LRP is the fallback, not the default</li><li><strong>Seamless upgrade</strong>: LRP → ICE upgrade happens transparently mid-connection</li><li><strong>Single port</strong>: WireGuard and ICE share UDP port 51820 via a filtering mux</li><li><strong>Multi-transport relay</strong>: LRP supports both TCP (HTTP upgrade) and QUIC (datagram)</li></ol><h2 id="architecture" tabindex="-1">Architecture <a class="header-anchor" href="#architecture" aria-label="Permalink to &quot;Architecture&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>                  Peer A                                  Peer B</span></span>
<span class="line"><span>              ┌───────────┐                          ┌───────────┐</span></span>
<span class="line"><span>   App ──────&gt;│   wf0     │                          │   wf0     │&lt;────── App</span></span>
<span class="line"><span>              │   TUN     │                          │   TUN     │</span></span>
<span class="line"><span>              └─────┬─────┘                          └─────┬─────┘</span></span>
<span class="line"><span>                    │                                      │</span></span>
<span class="line"><span>              ┌─────▼─────┐                          ┌─────▼─────┐</span></span>
<span class="line"><span>              │ WireGuard │                          │ WireGuard │</span></span>
<span class="line"><span>              │  Device   │                          │  Device   │</span></span>
<span class="line"><span>              └─────┬─────┘                          └─────┬─────┘</span></span>
<span class="line"><span>                    │                                      │</span></span>
<span class="line"><span>        ┌───────────▼───────────┐              ┌───────────▼───────────┐</span></span>
<span class="line"><span>        │   DefaultBind         │              │   DefaultBind         │</span></span>
<span class="line"><span>        │                       │              │                       │</span></span>
<span class="line"><span>        │  Send: ICE or LRP ────┼──────────────┼──&gt; Send: ICE or LRP   │</span></span>
<span class="line"><span>        │  Recv: passthru + LRP │              │  Recv: passthru + LRP │</span></span>
<span class="line"><span>        └───┬───────────────┬───┘              └───┬───────────────┬───┘</span></span>
<span class="line"><span>            │               │                      │               │</span></span>
<span class="line"><span>     ┌──────▼──────┐  ┌─────▼──────┐       ┌──────▼──────┐  ┌─────▼──────┐</span></span>
<span class="line"><span>     │ Filtering   │  │ LRP Client │       │ Filtering   │  │ LRP Client │</span></span>
<span class="line"><span>     │ UDPMux      │  │ (TCP/QUIC) │       │ UDPMux      │  │ (TCP/QUIC) │</span></span>
<span class="line"><span>     │ :51820      │  └─────┬──────┘       │ :51820      │  └─────┬──────┘</span></span>
<span class="line"><span>     └──────┬──────┘        │              └──────┬──────┘        │</span></span>
<span class="line"><span>            │               │                     │               │</span></span>
<span class="line"><span>     ┌──────▼──────┐  ┌─────▼──────┐       ┌──────▼──────┐  ┌─────▼──────┐</span></span>
<span class="line"><span>     │ ICE Agent   │  │            │       │ ICE Agent   │  │            │</span></span>
<span class="line"><span>     │ (pion/ice)  │  │  ┌─────────▼──┐    │ (pion/ice)  │  │  ┌─────────▼──┐</span></span>
<span class="line"><span>     └─────────────┘  │  │  Relay     │    └─────────────┘  │  │  Relay     │</span></span>
<span class="line"><span>                      │  │  Server    │                      │  │  Server    │</span></span>
<span class="line"><span>                      └──┤            ├──────────────────────┘  │            │</span></span>
<span class="line"><span>                         └────────────┘                         └────────────┘</span></span>
<span class="line"><span></span></span>
<span class="line"><span>         Signaling: NATS (lattice.signals.peers.&lt;PeerID&gt;)</span></span></code></pre></div><hr><h2 id="_1-ice-nat-traversal" tabindex="-1">1. ICE / NAT Traversal <a class="header-anchor" href="#_1-ice-nat-traversal" aria-label="Permalink to &quot;1. ICE / NAT Traversal&quot;">​</a></h2><h3 id="stack" tabindex="-1">Stack <a class="header-anchor" href="#stack" aria-label="Permalink to &quot;Stack&quot;">​</a></h3><p>Built on <a href="https://github.com/pion/ice" target="_blank" rel="noreferrer">pion/ice v4</a>.</p><h3 id="candidate-types" tabindex="-1">Candidate Types <a class="header-anchor" href="#candidate-types" aria-label="Permalink to &quot;Candidate Types&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Type</th><th>Support</th></tr></thead><tbody><tr><td>Host</td><td>Always</td></tr><tr><td>Server Reflexive (srflx)</td><td>Always (STUN: <code>stun.alattice.io:3478</code>)</td></tr><tr><td>Relay (TURN)</td><td>Pro only</td></tr></tbody></table><h3 id="filteringudpmux-—-shared-socket" tabindex="-1">FilteringUDPMux — Shared Socket <a class="header-anchor" href="#filteringudpmux-—-shared-socket" aria-label="Permalink to &quot;FilteringUDPMux — Shared Socket&quot;">​</a></h3><p>WireGuard and ICE share UDP port 51820. This creates a race — both want to read from the same socket. <code>FilteringUDPMux</code> solves this:</p><ol><li><code>readLoop</code> is the <strong>sole reader</strong> of the real UDP socket</li><li>Incoming packets classified: <ul><li><strong>STUN</strong> (detected via <code>stun.IsMessage</code>) → injected into <code>ChanPacketConn</code> → feeds pion/ice&#39;s mux</li><li><strong>Non-STUN</strong> (WireGuard encrypted) → forwarded through <code>passThroughCh</code> → WireGuard <code>DefaultBind</code> receive path</li></ul></li><li>Two instances: IPv4 and IPv6</li></ol><h3 id="ice-agent-configuration" tabindex="-1">ICE Agent Configuration <a class="header-anchor" href="#ice-agent-configuration" aria-label="Permalink to &quot;ICE Agent Configuration&quot;">​</a></h3><ul><li>Interface filter: excludes virtual (docker, veth, br-, wf*)</li><li>Uses <code>FilteringUDPMux.UDPMuxSrflx()</code> for server-reflexive candidates</li><li>Disconnected timeout: 10s; Failed timeout: 15s</li><li><code>OnConnectionStateChange</code>: Failed → triggers <code>Close()</code> → returns <code>ErrDialerClosed</code></li><li><code>OnCandidate</code>: each gathered candidate sent as <code>OFFER</code> signaling packet via NATS</li></ul><h3 id="candidate-exchange" tabindex="-1">Candidate Exchange <a class="header-anchor" href="#candidate-exchange" aria-label="Permalink to &quot;Candidate Exchange&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Initiator (higher PeerID numeric)</span></span>
<span class="line"><span>  │  HANDSHAKE_SYN (every 2s)</span></span>
<span class="line"><span>  ├──────────────────────────────&gt;</span></span>
<span class="line"><span>  │                               Responder</span></span>
<span class="line"><span>  │                               │  HANDSHAKE_ACK</span></span>
<span class="line"><span>  │  &lt;────────────────────────────┤</span></span>
<span class="line"><span>  │  OFFER (ICE candidate)        │</span></span>
<span class="line"><span>  ├──────────────────────────────&gt;│  OFFER (ICE candidate)</span></span>
<span class="line"><span>  │                               ├──────────────────────────────&gt;</span></span>
<span class="line"><span>  │  ANSWER (ICE candidate)       │</span></span>
<span class="line"><span>  &lt;───────────────────────────────┤</span></span>
<span class="line"><span>  │        ICE Connected          │</span></span></code></pre></div><ol><li>Initiator (higher <code>PeerID.ToUint64()</code>) sends <code>HANDSHAKE_SYN</code> via NATS every 2s</li><li>Responder receives SYN, sends <code>HANDSHAKE_ACK</code>, creates ICE agent, begins <code>GatherCandidates()</code></li><li>Initiator receives ACK, cancels SYN timer, begins <code>GatherCandidates()</code></li><li>Candidates exchanged via <code>OFFER</code>/<code>ANSWER</code> signaling packets</li><li><code>Dial()</code> unblocks on first candidate → <code>StartDial()</code>/<code>StartAccept()</code> → <code>AwaitConnect()</code></li></ol><hr><h2 id="_2-lrp-lattice-relay-protocol" tabindex="-1">2. LRP (Lattice Relay Protocol) <a class="header-anchor" href="#_2-lrp-lattice-relay-protocol" aria-label="Permalink to &quot;2. LRP (Lattice Relay Protocol)&quot;">​</a></h2><h3 id="protocol-frame-12-bytes-little-endian" tabindex="-1">Protocol Frame (12 bytes, little-endian) <a class="header-anchor" href="#protocol-frame-12-bytes-little-endian" aria-label="Permalink to &quot;Protocol Frame (12 bytes, little-endian)&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>┌──────────────┬──────────────┬──────┬──────────────┬──────────┐</span></span>
<span class="line"><span>│ Seq (2B)     │ PayloadLen   │ Cmd  │ ToID (4B)    │ Reserved │</span></span>
<span class="line"><span>│ uint16       │ (4B) uint32  │ (1B) │ uint32       │ (1B)     │</span></span>
<span class="line"><span>└──────────────┴──────────────┴──────┴──────────────┴──────────┘</span></span></code></pre></div><p><strong>Commands:</strong></p><table tabindex="0"><thead><tr><th>Cmd</th><th>Name</th><th>Purpose</th></tr></thead><tbody><tr><td><code>0x01</code></td><td>Register</td><td>Session registration (fromId → relay)</td></tr><tr><td><code>0x02</code></td><td>Forward</td><td>Relay WireGuard payload to target peer</td></tr><tr><td><code>0x03</code></td><td>KeepAlive</td><td>Reset 30s read deadline</td></tr><tr><td><code>0x04</code></td><td>Probe</td><td>Connectivity probe to target peer</td></tr></tbody></table><h3 id="server-architecture" tabindex="-1">Server Architecture <a class="header-anchor" href="#server-architecture" aria-label="Permalink to &quot;Server Architecture&quot;">​</a></h3><p>Two transport modes run alongside, sharing the same <code>SessionManager</code>:</p><p><strong>TCP server</strong> (<code>lrp_server.go</code>):</p><ol><li>HTTP listener → client performs <code>Upgrade: bolt</code> handshake</li><li>Server hijacks connection → reads Register frame → registers session</li><li>Main loop: KeepAlive (reset deadline) | Forward/Probe (relay via <code>SessionManager</code>)</li></ol><p><strong>QUIC server</strong> (<code>lrp_server_quic.go</code>):</p><ol><li><code>quic.ListenAddr</code> with self-signed TLS (ALPN: &quot;lrp&quot;)</li><li>Accept QUIC connection → read control stream → Register frame</li><li><code>relayDatagrams</code> goroutine: Forward/Probe via QUIC datagrams</li><li><code>handleControlStream</code>: KeepAlive on control stream</li></ol><h3 id="client-architecture" tabindex="-1">Client Architecture <a class="header-anchor" href="#client-architecture" aria-label="Permalink to &quot;Client Architecture&quot;">​</a></h3><p><strong>TCP client</strong> (<code>lrp_client_tcp.go</code>):</p><ul><li>TCP connect → HTTP Upgrade (<code>Upgrade: lrp</code>) → Register frame</li><li><code>writerLoop</code> goroutine: coalesced writes from <code>sendCh</code></li><li><code>keepaliveLoop</code>: KeepAlive every 20s</li><li><code>ReceiveFunc()</code>: reads LRP frames, returns Forward as WireGuard packets with <code>LRPEndpoint{TransportType: LRP}</code></li></ul><p><strong>QUIC client</strong> (<code>lrp_client_quic.go</code>):</p><ul><li>QUIC dial with <code>EnableDatagrams: true</code></li><li>Control stream for registration</li><li><code>Send()</code>: frames as QUIC datagrams</li><li><code>ReceiveFunc()</code>: reads QUIC datagrams, classifies Forward/Probe</li></ul><h3 id="session-manager" tabindex="-1">Session Manager <a class="header-anchor" href="#session-manager" aria-label="Permalink to &quot;Session Manager&quot;">​</a></h3><p>Central session registry for relay forwarding:</p><ul><li>Registers each connected client as <code>fromId → stream/conn</code></li><li><code>Relay(fromId, targetId, payload)</code>: looks up target session, forwards frame</li><li>Shared across TCP and QUIC servers</li></ul><hr><h2 id="_3-wireguard-integration" tabindex="-1">3. WireGuard Integration <a class="header-anchor" href="#_3-wireguard-integration" aria-label="Permalink to &quot;3. WireGuard Integration&quot;">​</a></h2><h3 id="defaultbind-wireguard-conn-bind" tabindex="-1">DefaultBind (WireGuard <code>conn.Bind</code>) <a class="header-anchor" href="#defaultbind-wireguard-conn-bind" aria-label="Permalink to &quot;DefaultBind (WireGuard \`conn.Bind\`)&quot;">​</a></h3><p>The critical integration point — implements WireGuard&#39;s binding interface:</p><p><strong>Receive path</strong> (<code>Open()</code>):</p><ul><li>Returns <code>ReceiveFunc</code> handlers reading from two sources: <ol><li><code>passThroughCh</code> — non-STUN packets from FilteringUDPMux (ICE/direct path)</li><li>LRP client&#39;s <code>ReceiveFunc()</code> — relayed packets</li></ol></li><li>All received packets wrapped in <code>LRPEndpoint</code></li></ul><p><strong>Send path</strong> (<code>Send()</code>):</p><ul><li>Checks <code>LRPEndpoint.TransportType</code>: <ul><li><code>ICE</code> → standard UDP socket (<code>WriteBatch</code> on Linux, <code>WriteMsgUDP</code> otherwise)</li><li><code>LRP</code> → <code>lrperClient.Send()</code> through relay</li></ul></li></ul><h3 id="lrpendpoint-wireguard-conn-endpoint" tabindex="-1">LRPEndpoint (WireGuard <code>conn.Endpoint</code>) <a class="header-anchor" href="#lrpendpoint-wireguard-conn-endpoint" aria-label="Permalink to &quot;LRPEndpoint (WireGuard \`conn.Endpoint\`)&quot;">​</a></h3><p>Unified endpoint carrying both physical and relay metadata:</p><table tabindex="0"><thead><tr><th>Method</th><th>ICE path</th><th>LRP path</th></tr></thead><tbody><tr><td><code>DstToBytes()</code></td><td>Marshaled <code>netip.AddrPort</code></td><td>Encoded <code>RemoteId</code> bytes</td></tr><tr><td><code>DstIP()</code></td><td>Real IP</td><td><code>IPv4Unspecified</code> (ID-based routing)</td></tr><tr><td><code>DstToString()</code></td><td>IP:port string</td><td>Relay address string</td></tr></tbody></table><p>Fake IPv6 prefix (<code>fd6c:7270::</code>) encodes RemoteId for <code>wg show</code> compatibility.</p><h3 id="wireguard-uapi-handler" tabindex="-1">WireGuard UAPI Handler <a class="header-anchor" href="#wireguard-uapi-handler" aria-label="Permalink to &quot;WireGuard UAPI Handler&quot;">​</a></h3><p><code>DeviceManager</code> runs a UAPI socket handler for standard WireGuard config operations (<code>set=1</code>, <code>get=1</code>), delegating directly to the <code>wg.Device</code> from the official library.</p><hr><h2 id="_4-transport-state-machine" tabindex="-1">4. Transport State Machine <a class="header-anchor" href="#_4-transport-state-machine" aria-label="Permalink to &quot;4. Transport State Machine&quot;">​</a></h2><h3 id="states" tabindex="-1">States <a class="header-anchor" href="#states" aria-label="Permalink to &quot;States&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Created → Probing → ICEReady / LRPReady → Failed → Closed</span></span>
<span class="line"><span>                   ↖                         ↓        ↗</span></span>
<span class="line"><span>                     └───────────────────────┘</span></span></code></pre></div><h3 id="allowed-transitions" tabindex="-1">Allowed Transitions <a class="header-anchor" href="#allowed-transitions" aria-label="Permalink to &quot;Allowed Transitions&quot;">​</a></h3><table tabindex="0"><thead><tr><th>From</th><th>To</th></tr></thead><tbody><tr><td>Created</td><td>Probing</td></tr><tr><td>Probing</td><td>ICEReady, LRPReady, Failed</td></tr><tr><td>LRPReady</td><td>ICEReady (upgrade), Failed, Closed</td></tr><tr><td>ICEReady</td><td>Failed, Closed</td></tr><tr><td>Failed</td><td>Probing (restart), Closed</td></tr></tbody></table><h3 id="probe-—-lifecycle-orchestrator" tabindex="-1">Probe — Lifecycle Orchestrator <a class="header-anchor" href="#probe-—-lifecycle-orchestrator" aria-label="Permalink to &quot;Probe — Lifecycle Orchestrator&quot;">​</a></h3><p>Each remote peer gets a <code>Probe</code> that manages connection lifecycle:</p><ol><li><strong><code>Start()</code></strong>: CAS on <code>running</code> flag → transition <code>Created → Probing</code> → launch <code>discover()</code></li><li><strong><code>discover()</code></strong>: Races ICE and LRP dialers concurrently <ul><li>Both call <code>Prepare()</code> (SYN every 2s, up to 60s) then <code>Dial()</code> (wait for candidate exchange)</li><li><strong>Race priority</strong>: ICE &gt; LRP (lower latency P2P preferred)</li><li>If LRP wins first → wait 500ms for ICE → if ICE arrives, discard LRP; otherwise use LRP and continue ICE in background</li></ul></li><li><strong><code>onSuccess()</code></strong>: Transition to <code>ICEReady</code> or <code>LRPReady</code></li><li><strong><code>handleUpgradeTransport()</code></strong>: LRP → ICE upgrade <ul><li>Replace <code>currentTransport</code> with ICE transport</li><li>Close old LRP transport after 2s delay</li><li>Transition <code>LRPReady → ICEReady</code></li></ul></li><li><strong><code>onFailure()</code></strong>: <ul><li><code>ErrDialerClosed</code>: clean session end → restart immediately</li><li>Other errors: track <code>firstFailureAt</code> → 60s unreachable → <code>Closed</code>; otherwise <code>Failed</code> → restart after 10s</li></ul></li><li><strong><code>restart()</code></strong>: Create fresh dialers, increment epoch (invalidates old goroutines), transition <code>Failed → Probing</code></li></ol><h3 id="connection-configurator-side-effects" tabindex="-1">Connection Configurator Side Effects <a class="header-anchor" href="#connection-configurator-side-effects" aria-label="Permalink to &quot;Connection Configurator Side Effects&quot;">​</a></h3><p>On state transitions, the <code>Configurator</code> applies WireGuard changes:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Probing → ICEReady/LRPReady:</span></span>
<span class="line"><span>  - SetEndpoint (WireGuard peer endpoint)</span></span>
<span class="line"><span>  - ApplyRoute (kernel route for peer VPN IP)</span></span>
<span class="line"><span>  - SetupNAT (NAT rules for wf0 interface)</span></span>
<span class="line"><span></span></span>
<span class="line"><span>LRPReady → ICEReady (upgrade):</span></span>
<span class="line"><span>  - SetEndpoint only (no duplicate peer/route/NAT)</span></span>
<span class="line"><span></span></span>
<span class="line"><span>→ Failed/Closed:</span></span>
<span class="line"><span>  - RemovePeer (clean up WireGuard state)</span></span></code></pre></div><h3 id="metrics" tabindex="-1">Metrics <a class="header-anchor" href="#metrics" aria-label="Permalink to &quot;Metrics&quot;">​</a></h3><p>Exposed via VictoriaMetrics:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>lattice_transport_state_changes_total{from, to}</span></span></code></pre></div><hr><h2 id="_5-signaling-nats" tabindex="-1">5. Signaling (NATS) <a class="header-anchor" href="#_5-signaling-nats" aria-label="Permalink to &quot;5. Signaling (NATS)&quot;">​</a></h2><h3 id="subjects" tabindex="-1">Subjects <a class="header-anchor" href="#subjects" aria-label="Permalink to &quot;Subjects&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>lattice.signals.peers.&lt;PeerID&gt;</span></span></code></pre></div><p>PeerID: first 8 bytes of WireGuard public key, encoded as uint64.</p><h3 id="protobuf-signal-packet" tabindex="-1">Protobuf Signal Packet <a class="header-anchor" href="#protobuf-signal-packet" aria-label="Permalink to &quot;Protobuf Signal Packet&quot;">​</a></h3><div class="language-protobuf vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">protobuf</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">message</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> SignalPacket</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    PacketType type;     </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// HANDSHAKE_SYN, HANDSHAKE_ACK, OFFER, ANSWER, MESSAGE</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    DialerType dialer;   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// ICE or LRP</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    uint64 sender_id;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    oneof</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> payload {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        Handshake handshake;  </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// peer info (JSON)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        Offer offer;          </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// ICE candidate or LRP offer</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        Offer answer;         </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// ICE candidate or LRP answer</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        Message message;      </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// config push from control plane</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><h3 id="flow" tabindex="-1">Flow <a class="header-anchor" href="#flow" aria-label="Permalink to &quot;Flow&quot;">​</a></h3><ol><li>Node subscribes to its own subject at startup</li><li>Control plane pushes config via <code>MESSAGE</code> packets → <code>MessageHandler</code> processes → <code>AddPeer</code> → creates <code>Probe</code></li><li>Peer-to-peer signaling via <code>Publish</code>/<code>RequestWithContext</code>: <ul><li><code>RequestWithContext</code> uses exponential backoff: 100ms → 200ms → 400ms → 800ms → 1600ms (5 retries)</li></ul></li><li>On NATS reconnect: re-register, re-apply network map</li></ol><h3 id="initiator-determination" tabindex="-1">Initiator Determination <a class="header-anchor" href="#initiator-determination" aria-label="Permalink to &quot;Initiator Determination&quot;">​</a></h3><div class="language-go vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">go</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">isInitiator </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> local.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ID</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">().</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ToUint64</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">() </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> remote.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ID</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">().</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ToUint64</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()</span></span></code></pre></div><p>Numeric comparison of uint64 PeerID — deterministic, no additional negotiation needed.</p><hr><h2 id="_6-end-to-end-datapath" tabindex="-1">6. End-to-End Datapath <a class="header-anchor" href="#_6-end-to-end-datapath" aria-label="Permalink to &quot;6. End-to-End Datapath&quot;">​</a></h2><h3 id="data-encrypted-traffic" tabindex="-1">Data (Encrypted Traffic) <a class="header-anchor" href="#data-encrypted-traffic" aria-label="Permalink to &quot;Data (Encrypted Traffic)&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>App on A → wf0 TUN → WireGuard encrypt → DefaultBind.Send()</span></span>
<span class="line"><span>  ├── ICE path: FilteringUDPMux → UDP :51820 ────────────────&gt; Peer B</span></span>
<span class="line"><span>  └── LRP path: LRP Forward frame → Relay Server → Peer B</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Peer B: DefaultBind.ReceiveFunc()</span></span>
<span class="line"><span>  ├── ICE path: passThroughCh ← FilteringUDPMux ← UDP :51820</span></span>
<span class="line"><span>  └── LRP path: LRP client ReceiveFunc ← Relay Server</span></span>
<span class="line"><span>  → WireGuard decrypt → wf0 TUN → App on B</span></span></code></pre></div><h3 id="signaling-connection-establishment" tabindex="-1">Signaling (Connection Establishment) <a class="header-anchor" href="#signaling-connection-establishment" aria-label="Permalink to &quot;Signaling (Connection Establishment)&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>1. Control plane → MESSAGE (NATS) → MessageHandler → AddPeer → Probe(StateCreated)</span></span>
<span class="line"><span>2. Probe.Start() → Probing → discover()</span></span>
<span class="line"><span>3. HANDSHAKE_SYN/ACK exchange via NATS (2s interval)</span></span>
<span class="line"><span>4. OFFER/ANSWER candidate exchange via NATS</span></span>
<span class="line"><span>5. Transport established → ICEReady/LRPReady</span></span>
<span class="line"><span>6. onSuccess() → WireGuard SetEndpoint + ApplyRoute + SetupNAT</span></span>
<span class="line"><span>7. [If LRP] → background ICE → upgrade to ICEReady when ready</span></span></code></pre></div><hr><h2 id="_7-pro-community-split" tabindex="-1">7. Pro/Community Split <a class="header-anchor" href="#_7-pro-community-split" aria-label="Permalink to &quot;7. Pro/Community Split&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Feature</th><th>Community</th><th>Pro</th></tr></thead><tbody><tr><td>ICE (host + srflx)</td><td>Yes</td><td>Yes</td></tr><tr><td>LRP relay (TCP + QUIC)</td><td>Yes</td><td>Yes</td></tr><tr><td>TURN relay</td><td>402 stub</td><td>Yes</td></tr><tr><td>State machine metrics</td><td>Yes</td><td>Yes</td></tr></tbody></table><hr><h2 id="file-layout" tabindex="-1">File Layout <a class="header-anchor" href="#file-layout" aria-label="Permalink to &quot;File Layout&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>internal/agent/infra/ice.go                 ICE agent factory, filtering mux</span></span>
<span class="line"><span>internal/agent/infra/mux_filter.go          FilteringUDPMux implementation</span></span>
<span class="line"><span>internal/agent/infra/chan_conn.go           ChanPacketConn (STUN injection)</span></span>
<span class="line"><span>internal/agent/infra/conn.go                DefaultBind (WireGuard conn.Bind)</span></span>
<span class="line"><span>internal/agent/infra/wrrp.go                LRPEndpoint (WireGuard conn.Endpoint)</span></span>
<span class="line"><span>internal/agent/infra/transport.go           SignalService + Probe interfaces</span></span>
<span class="line"><span>internal/agent/infra/peer.go                PeerID, PeerIdentity</span></span>
<span class="line"><span>internal/agent/infra/device_conf.go         WireGuard DeviceConf</span></span>
<span class="line"><span>internal/agent/wireguard/wg.go              DeviceManager, UAPI handler (Unix)</span></span>
<span class="line"><span>internal/agent/wireguard/wg_windows.go      DeviceManager (Windows)</span></span>
<span class="line"><span>internal/agent/wireguard/status.go          WireGuard status reporting</span></span>
<span class="line"><span>internal/agent/wireguard/handshake_watcher.go  First handshake watcher</span></span>
<span class="line"><span>internal/server/transport/ice_dialer.go     ICE Dialer (pion/ice agent)</span></span>
<span class="line"><span>internal/server/transport/lrp_dialer.go     LRP Dialer (relay negotiation)</span></span>
<span class="line"><span>internal/server/transport/state_machine.go  Transport state machine</span></span>
<span class="line"><span>internal/server/transport/state.go          ConnectionState enum</span></span>
<span class="line"><span>internal/server/transport/probe.go          Probe lifecycle orchestrator</span></span>
<span class="line"><span>internal/server/transport/probe_factory.go  ProbeFactory (all probes)</span></span>
<span class="line"><span>internal/relay/lrp_protocol.go             LRP frame definition (12 bytes)</span></span>
<span class="line"><span>internal/relay/lrp_server.go               TCP relay server (HTTP upgrade)</span></span>
<span class="line"><span>internal/relay/lrp_server_quic.go          QUIC relay server</span></span>
<span class="line"><span>internal/relay/lrp_client_tcp.go           TCP relay client</span></span>
<span class="line"><span>internal/relay/lrp_client_quic.go          QUIC relay client</span></span>
<span class="line"><span>internal/relay/lrp_client.go               Shared relay client logic</span></span>
<span class="line"><span>internal/relay/session_manager.go          Session registration + forwarding</span></span>
<span class="line"><span>internal/relay/conn.go                     ReadWriterConn wrapper</span></span>
<span class="line"><span>internal/relay/pool.go                     Buffer pools</span></span>
<span class="line"><span>internal/relay/stream.go                   Stream/Session interfaces</span></span>
<span class="line"><span>internal/relay/turn_server.go              TURN server (Pro)</span></span>
<span class="line"><span>internal/relay/turn_client.go              TURN client (Pro)</span></span>
<span class="line"><span>internal/relay/turn_community.go           TURN community stub</span></span>
<span class="line"><span>internal/server/nats/nats.go               NATS signal service</span></span>
<span class="line"><span>internal/grpc/signal.pb.go                 Protobuf signal definitions</span></span>
<span class="line"><span>internal/agent/tunnel/drp.go               DRP frame types (legacy)</span></span>
<span class="line"><span>internal/agent/tunnel/endpoint.go          MagicEndpoint</span></span></code></pre></div>`,95)])])}const u=e(i,[["render",r]]);export{g as __pageData,u as default};
