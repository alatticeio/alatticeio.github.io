import{_ as a,o as s,c as i,a0 as t}from"./chunks/framework.DoRPXVp_.js";const g=JSON.parse('{"title":"Unified Agent Sandbox Design","description":"","frontmatter":{},"headers":[],"relativePath":"archive/sandbox-specs/2026-05-27-unified-agent-sandbox-design.md","filePath":"archive/sandbox-specs/2026-05-27-unified-agent-sandbox-design.md"}'),n={name:"archive/sandbox-specs/2026-05-27-unified-agent-sandbox-design.md"};function o(r,e,l,d,c,h){return s(),i("div",null,[...e[0]||(e[0]=[t(`<h1 id="unified-agent-sandbox-design" tabindex="-1">Unified Agent Sandbox Design <a class="header-anchor" href="#unified-agent-sandbox-design" aria-label="Permalink to &quot;Unified Agent Sandbox Design&quot;">​</a></h1><p><strong>Date</strong>: 2026-05-27 <strong>Status</strong>: Approved <strong>Branch</strong>: dev</p><h2 id="background" tabindex="-1">Background <a class="header-anchor" href="#background" aria-label="Permalink to &quot;Background&quot;">​</a></h2><p>The current sandbox implementation has three divergent paths:</p><table tabindex="0"><thead><tr><th>Mode</th><th>Command</th><th>Network</th><th>Platform</th></tr></thead><tbody><tr><td>Community sandbox</td><td><code>sandbox start</code></td><td>gVisor netstack library (no SOCKS5)</td><td>Linux</td></tr><tr><td>Pod sandbox (PRO)</td><td><code>sandbox run --mode pod</code></td><td>gVisor netstack library + SOCKS5</td><td>PRO + Linux</td></tr><tr><td>gVisor sandbox (PRO)</td><td><code>sandbox run --mode gvisor</code></td><td>Phase 1: kernel wg0 + Phase 2: runsc</td><td>PRO + Linux</td></tr></tbody></table><p>Problems:</p><ul><li>The <code>RunscDriver</code> (gvisor mode) creates a <strong>real kernel wg0</strong> in Phase 1, which is exactly what we want to eliminate for AI agent workloads.</li><li>Three separate code paths with divergent behaviour; the mode flag is confusing.</li><li>Mac / non-Linux users cannot run any sandbox.</li><li>SOCKS5 adds an unnecessary layer that leaks proxy awareness into the AI agent.</li></ul><h2 id="goal" tabindex="-1">Goal <a class="header-anchor" href="#goal" aria-label="Permalink to &quot;Goal&quot;">​</a></h2><p>Unify all sandbox paths into a single approach: <strong>full gVisor userspace, no kernel wg0, works anywhere Docker runs</strong>.</p><ul><li><code>lattice up</code> is untouched — regular agents continue to use the kernel WireGuard interface.</li><li>All AI agent sandbox use cases move to the new <code>lattice agent</code> command family.</li><li>The <code>lattice sandbox</code> command tree is removed entirely.</li></ul><h2 id="two-scenarios" tabindex="-1">Two Scenarios <a class="header-anchor" href="#two-scenarios" aria-label="Permalink to &quot;Two Scenarios&quot;">​</a></h2><h3 id="scenario-a-—-single-container-docker-local-binary" tabindex="-1">Scenario A — Single container (Docker / local binary) <a class="header-anchor" href="#scenario-a-—-single-container-docker-local-binary" aria-label="Permalink to &quot;Scenario A — Single container (Docker / local binary)&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>docker run --runtime=runsc \\</span></span>
<span class="line"><span>  -e LATTICE_SERVER_URL=http://latticed:8080 \\</span></span>
<span class="line"><span>  ghcr.io/alattice/lattice \\</span></span>
<span class="line"><span>  agent run my-agent --server-url ... --token lt-xxx \\</span></span>
<span class="line"><span>  -- python agent.py</span></span></code></pre></div><p>The entire container runs under gVisor (<code>--runtime=runsc</code>). Inside the container:</p><ol><li><code>lattice agent run</code> registers with the control plane via NATS.</li><li>WireGuard uses <code>/dev/net/tun</code> — gVisor virtualises this device, so <code>wg0</code> exists only inside gVisor&#39;s userspace netstack (no kernel interface is created on the host).</li><li>ICE/LRP peer connections are established.</li><li>After <code>--ready-wait</code>, ambient capabilities are cleared, then <code>syscall.Exec</code> replaces the process with the AI agent binary.</li><li>The AI agent inherits the gVisor sandbox. All its <code>connect()</code> syscalls are intercepted by the gVisor sentry and routed through gVisor&#39;s netstack → virtual wg0 → WireGuard overlay.</li></ol><p>No SOCKS5. No kernel wg0. No special routing configuration.</p><p><strong>Volume mounts</strong>: if the user mounts host directories (<code>-v /workspace:/workspace</code>), the AI agent can read/write those paths. gVisor mediates access at the syscall boundary (no kernel exploit surface), but the mounted data itself is accessible. Users should mount only the directories the agent needs.</p><h3 id="scenario-b-—-kubernetes-sidecar-standard-runtime-no-gvisor-runtimeclass-required" tabindex="-1">Scenario B — Kubernetes sidecar (standard runtime, no gVisor RuntimeClass required) <a class="header-anchor" href="#scenario-b-—-kubernetes-sidecar-standard-runtime-no-gvisor-runtimeclass-required" aria-label="Permalink to &quot;Scenario B — Kubernetes sidecar (standard runtime, no gVisor RuntimeClass required)&quot;">​</a></h3><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">initContainers</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">- </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">name</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">lattice-init</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  image</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">ghcr.io/alattice/lattice</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  command</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;lattice&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;agent&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;init&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  securityContext</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    capabilities</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">      add</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;NET_ADMIN&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">containers</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">- </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">name</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">lattice-sidecar</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  image</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">ghcr.io/alattice/lattice</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  command</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;lattice&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;agent&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;sidecar&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;my-agent&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">            &quot;--server-url&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;http://latticed:8080&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;--token&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;lt-xxx&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  securityContext</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    runAsUser</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1337</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">- </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">name</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">ai-agent</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  image</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">my-agent:latest</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  # zero changes required</span></span></code></pre></div><p>Flow:</p><ol><li><strong>Init container</strong> (<code>lattice agent init</code>): writes iptables REDIRECT rules — all outbound TCP from the pod is redirected to port 15001, except traffic originating from UID 1337. Then exits 0.</li><li><strong>Sidecar</strong> (<code>lattice agent sidecar</code>, UID 1337): registers with control plane, creates a gVisor netstack using the embedded library (<code>gvisor.New</code> + <code>gvisor.NewTUNAdapter</code>), starts a transparent TCP proxy on <code>0.0.0.0:15001</code>.</li><li><strong>AI agent container</strong>: makes TCP connections as normal. The kernel redirects them to port 15001. The sidecar reads the original destination via <code>SO_ORIGINAL_DST</code>, dials through the gVisor netstack (which has WireGuard configured), and proxies the data.</li><li>Sidecar&#39;s own outbound traffic (UDP WireGuard, NATS) originates from UID 1337, bypassing the iptables redirect.</li></ol><p>No changes to the AI agent image. No <code>ALL_PROXY</code> env var required.</p><h2 id="cli-design" tabindex="-1">CLI Design <a class="header-anchor" href="#cli-design" aria-label="Permalink to &quot;CLI Design&quot;">​</a></h2><h3 id="lattice-agent-run" tabindex="-1"><code>lattice agent run</code> <a class="header-anchor" href="#lattice-agent-run" aria-label="Permalink to &quot;\`lattice agent run\`&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>lattice agent run &lt;name&gt; \\</span></span>
<span class="line"><span>  --server-url &lt;url&gt; \\</span></span>
<span class="line"><span>  --token &lt;token&gt; \\</span></span>
<span class="line"><span>  [--ready-wait &lt;duration&gt;]    default: 3s</span></span>
<span class="line"><span>  [--egress-allow &lt;CIDRs&gt;]     PRO only; comma-separated</span></span>
<span class="line"><span>  [--egress-default-deny]      PRO only</span></span>
<span class="line"><span>  -- &lt;command&gt; [args...]</span></span></code></pre></div><p>Intended as the entrypoint of a <code>--runtime=runsc</code> container. Registers, configures WireGuard, waits for peers, then <code>syscall.Exec</code>s the given command.</p><h3 id="lattice-agent-sidecar" tabindex="-1"><code>lattice agent sidecar</code> <a class="header-anchor" href="#lattice-agent-sidecar" aria-label="Permalink to &quot;\`lattice agent sidecar\`&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>lattice agent sidecar &lt;name&gt; \\</span></span>
<span class="line"><span>  --server-url &lt;url&gt; \\</span></span>
<span class="line"><span>  --token &lt;token&gt; \\</span></span>
<span class="line"><span>  [--proxy-port &lt;port&gt;]        default: 15001</span></span>
<span class="line"><span>  [--egress-allow &lt;CIDRs&gt;]     PRO only</span></span>
<span class="line"><span>  [--egress-default-deny]      PRO only</span></span></code></pre></div><p>Runs as a Kubernetes sidecar. Embeds gVisor netstack for WireGuard (no kernel wg0). Listens as a transparent TCP proxy; forwards connections through the overlay using <code>SO_ORIGINAL_DST</code>.</p><p>Must run as a UID excluded from the pod&#39;s iptables redirect rules (default UID 1337).</p><h3 id="lattice-agent-init" tabindex="-1"><code>lattice agent init</code> <a class="header-anchor" href="#lattice-agent-init" aria-label="Permalink to &quot;\`lattice agent init\`&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>lattice agent init \\</span></span>
<span class="line"><span>  [--proxy-port &lt;port&gt;]        default: 15001</span></span>
<span class="line"><span>  [--skip-uid &lt;uid&gt;]           default: 1337</span></span></code></pre></div><p>Kubernetes init container. Writes iptables REDIRECT rules and exits 0. Requires <code>NET_ADMIN</code> capability. Stateless — safe to re-run.</p><h2 id="internal-implementation" tabindex="-1">Internal Implementation <a class="header-anchor" href="#internal-implementation" aria-label="Permalink to &quot;Internal Implementation&quot;">​</a></h2><h3 id="file-changes" tabindex="-1">File changes <a class="header-anchor" href="#file-changes" aria-label="Permalink to &quot;File changes&quot;">​</a></h3><p><strong>Delete</strong>:</p><ul><li><code>cmd/lattice/cmd/sandbox/</code> — entire package</li><li><code>internal/agent/runsc/</code> — RunscDriver two-phase orchestration, no longer needed</li></ul><p><strong>Add</strong>:</p><ul><li><code>cmd/lattice/cmd/agent/agent.go</code> — top-level <code>lattice agent</code> cobra command</li><li><code>cmd/lattice/cmd/agent/run.go</code> + <code>run_community.go</code> — <code>agent run</code> (community: no policy)</li><li><code>cmd/lattice/cmd/agent/run_pro.go</code> — <code>agent run</code> with egress policy flags (PRO + linux)</li><li><code>cmd/lattice/cmd/agent/sidecar.go</code> + <code>sidecar_community.go</code> — <code>agent sidecar</code></li><li><code>cmd/lattice/cmd/agent/sidecar_pro.go</code> — sidecar with egress policy (PRO + linux)</li><li><code>cmd/lattice/cmd/agent/init.go</code> — <code>agent init</code> (iptables, linux only)</li><li><code>cmd/lattice/cmd/agent/shared.go</code> — credential persistence (moved from sandbox_shared.go)</li></ul><p><strong>Keep / reuse unchanged</strong>:</p><ul><li><code>internal/agent/gvisor/</code> — gVisor netstack library (used by sidecar mode)</li><li><code>internal/agent/sandbox_register.go</code> — NATS registration helpers</li></ul><p><strong>Modify</strong>:</p><ul><li><code>cmd/lattice/cmd/root.go</code> — replace <code>sandbox.SandboxCmd()</code> with <code>agent.AgentCmd()</code></li></ul><h3 id="agent-run-implementation-note" tabindex="-1"><code>agent run</code> implementation note <a class="header-anchor" href="#agent-run-implementation-note" aria-label="Permalink to &quot;\`agent run\` implementation note&quot;">​</a></h3><p><code>sandbox_agent.go</code> already contains essentially the right logic: it registers via NATS, creates a WireGuard node without <code>CustomTUN</code> (so wireguard-go uses <code>/dev/net/tun</code>, which gVisor virtualises), and <code>syscall.Exec</code>s the AI agent as PID 1. The new <code>agent run</code> is a direct lift of this logic, promoted to a first-class command and stripped of the <code>--network=host</code> dependency. The outer <code>RunscDriver</code> (which created the kernel wg0 in Phase 1) is deleted entirely.</p><h3 id="agent-sidecar-implementation-note" tabindex="-1"><code>agent sidecar</code> implementation note <a class="header-anchor" href="#agent-sidecar-implementation-note" aria-label="Permalink to &quot;\`agent sidecar\` implementation note&quot;">​</a></h3><p>The transparent proxy listener reads <code>SO_ORIGINAL_DST</code> on each accepted connection to retrieve the original destination before the iptables REDIRECT. It then dials that destination through the gVisor netstack (which has WireGuard peers configured), and copies data bidirectionally. The gVisor netstack setup is identical to the existing <code>PodDriver</code> in <code>driver_pod.go</code>, minus the SOCKS5 server.</p><h2 id="pro-community-split" tabindex="-1">PRO / Community split <a class="header-anchor" href="#pro-community-split" aria-label="Permalink to &quot;PRO / Community split&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Feature</th><th>Community</th><th>PRO</th></tr></thead><tbody><tr><td><code>agent run</code></td><td>All egress allowed</td><td>+ <code>--egress-allow</code> / <code>--egress-default-deny</code> enforced via gVisor virtual iptables</td></tr><tr><td><code>agent sidecar</code></td><td>All egress allowed</td><td>+ egress policy enforced at transparent proxy layer</td></tr><tr><td><code>agent init</code></td><td>Available</td><td>Available</td></tr></tbody></table><p>Build tags follow the existing pattern: <code>//go:build pro &amp;&amp; linux</code> for PRO-only files, <code>//go:build !pro</code> for community stubs.</p><h2 id="out-of-scope" tabindex="-1">Out of Scope <a class="header-anchor" href="#out-of-scope" aria-label="Permalink to &quot;Out of Scope&quot;">​</a></h2><ul><li><strong>Filesystem access controls</strong>: volume mount permissions are the user&#39;s responsibility at <code>docker run -v</code> / Pod spec level. A <code>--mount</code> whitelist flag is a candidate for a future PRO enhancement.</li><li><strong>K8s with gVisor RuntimeClass</strong>: if the cluster has <code>runtimeClassName: gvisor</code> available, the sidecar + iptables approach is unnecessary (all containers share gVisor&#39;s netstack). This is a documented alternative, not a primary target for this change.</li><li><strong>Windows / WSL2</strong>: supported via Docker Desktop (<code>--runtime=runsc</code> requires gVisor on the Docker Desktop VM). No code changes required.</li></ul>`,52)])])}const u=a(n,[["render",o]]);export{g as __pageData,u as default};
